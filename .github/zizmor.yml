# Please see the documentation for all configuration options:
# https://docs.zizmor.sh/configuration/

rules:
  dangerous-triggers:
    ignore:
      # `pull_request_target` is used safely here: The workflow checks whether
      # the PR is from a fork before running, and write access is gated behind
      # the `default-branch` environment.
      - update-changelogs.yml:3:1
      # `pull_request_target` is used safely here: The workflow never checks out
      # or executes any code from the pull request. It only reads the PR's file
      # list and hands the PR number to the CLA action, which talks to the
      # GitHub API and nothing else.
      - cla.yml:3:1

  dependabot-cooldown:
    config:
      # Change the minimum allowed cooldown period for Dependabot to 3 days.
      days: 3

  unpinned-uses:
    config:
      policies:
        # Allow `actions/*` and `MetaMask/*` to be pinned to a version instead
        # of only to a commit hash.
        actions/*: ref-pin
        MetaMask/*: ref-pin
